import React from "react";
import "./Stock.css";

const Navbar = () => (
  <nav className="navbar">
    <a href="#">INICIO</a>
    <a href="#">PRODUCTOS</a>
    <a href="#">CONTACTO</a>
    <a href="#">PREGUNTAS FRECUENTES</a>
    <a href="#">STOCK</a>
  </nav>
);

export default Navbar;
